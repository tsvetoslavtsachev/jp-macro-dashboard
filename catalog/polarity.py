"""
catalog/polarity.py
===================
Полярност на сериите за Japan Macro Dashboard — единен източник на истина за
„какво значи здраво", отделен от каталога нарочно: каталогът казва КАКВО се
мери, полярността — КАК се съди.

Четири типа полярност (фамилен стандарт):
  · +1                  — по-високо = по-здраво (растеж, износ)
  · -1                  — по-високо = по-нездраво (безработица, лихви)
  · ("U", "self")       — отклонение в двете посоки е лошо, център = медианата
  · ("U", "target", X)  — U около фиксирана цел (инфлацията около 2%)
  · ("OPT", lo, hi, s)  — АБСОЛЮТНА оптимална зона: плато в [lo,hi], спад по
                          1σ на всеки s пункта извън; median/MAD НЕ участват

OPT зоните са фиксирани от ДАННИ-ПАС на живо (05.08.2026, фаза 6) върху
трансформираните серии — такива, каквито ги вижда скорингът. Приемният гейт:
балонната ера 1985-90 свети, дефлационната 1995-2012 и абеномиката 2013-19
мълчат. Замразената таблица (не се прекалибрира без нов мандат):

  Серия              Зона (lo,hi,s)   Балон 85-90  Дефл. 95-12  Абен. 13-19  Тек. 22→
  JP_RPPI            (0.0, 4.0, 4.0)   16/24        0/72         0/28         8/16
  JP_CREDIT_GDP_NFC  (0.0, 4.5, 3.0)   10/24        4/72 ⚠       0/28         0/16
  JP_CREDIT_GDP_HH   (0.0, 3.0, 3.0)   15/24        4/72 ⚠       0/28         0/16

⚠ Четирите дефлационни тримесечия и в двата кредитни крака са 2008Q4-2010Q1 —
GFC ефектът на ЗНАМЕНАТЕЛЯ (БВП пада → съотношението скача), известната
слабост на credit/GDP темпа. Декларира се, не се крие: гейтът брои бум-епохата
срещу СПОКОЙНИТЕ епохи, а кризисният фалшив позитив е отделен клас, четим от
контекста (в криза композитът и без това крещи от другите лещи).

Котвите на зоните: hi = максимумът на най-топлата СПОКОЙНА епоха, закръглен
нагоре (имоти: абеномика max 3.91 → 4.0 · NFC: абеномика max 4.14 → 4.5 ·
HH: абеномика max 2.03 → 3.0). Ръстът на credit/GDP съотношението Е
кредит-минус-БВП разликата — положителен темп значи кредит по-бърз от
икономиката, устойчиво над спокойния таван = бум.
"""
from __future__ import annotations

from typing import Any

INFLATION_TARGET = 2.0  # % — целта на BOJ (обявена 01.2013, потвърждавана оттогава)
U_BAND = 1.0            # толерантна лента в σ, преди здравето да падне

POLARITY: dict[str, Any] = {
    # ── GROWTH ────────────────────────────────────────────────────────────────
    "JP_RGDP": +1,
    "JP_INDPRO": +1,
    "JP_RETAIL": +1,

    # ── INFLATION (U около целта на BOJ: 2%) ──────────────────────────────────
    # Отклонение в ДВЕТЕ посоки е нездраво — Япония е единствената икономика
    # във фамилията, където дефлационната страна на U-то не е теория, а три
    # изживени десетилетия.
    "JP_CPI": ("U", "target", INFLATION_TARGET),
    "JP_CPI_CORE": ("U", "target", INFLATION_TARGET),

    # ── LABOR ─────────────────────────────────────────────────────────────────
    "JP_UNRATE": -1,          # висока безработица = нездраво
    # Заплатният ръст: РЕШЕНО с фаза 6 данни-паса — остава ЛИНЕЕН, без зона.
    # Пасът показа балон max 10.12 срещу текущи ~5, но заплатният импулс е
    # точно това, което BOJ чака 30 години (шунто възраждането) — OPT зона би
    # етикетирала успеха на политиката като „прегряване". Ако спиралата
    # избяга, инфлационното U около 2% ще го каже от своята леща.
    "JP_EARNINGS_MFG": +1,

    # ── CREDIT ────────────────────────────────────────────────────────────────
    "JP_10Y": -1,             # по-висока цена на дълга = по-стегнато
    # Кредитният темп (г/г на BIS %GDP съотношението): РЕШЕНО с фаза 6
    # данни-паса. Плато 0..hi = здрава експанзия (кредит в крак с икономиката),
    # над hi = бум (балонната ера), под 0 = дълговото разлистване. Котва:
    # максимумът на абеномиката (най-топлата спокойна епоха), закръглен.
    "JP_CREDIT_GDP_NFC": ("OPT", 0.0, 4.5, 3.0),
    "JP_CREDIT_GDP_HH": ("OPT", 0.0, 3.0, 3.0),

    # ── EXTERNAL ──────────────────────────────────────────────────────────────
    "JP_TRADE_BAL": +1,       # по-силен баланс = по-здраво
    "JP_EXPORTS": +1,
    "JP_CA": +1,              # трите външни гласа са ЕДНА peer-група (един шок)

    # ── PROPERTY ──────────────────────────────────────────────────────────────
    # Ценовият ръст: РЕШЕНО с фаза 6 данни-паса. Плато 0..4 = ръст в крак с
    # дохода; над 4 = прегряване (балонният гейт: 16/24 свети); под 0 =
    # дефлационното дъно (1995-2012 med −2.37 — балансовата рецесия Е
    # нездраве, не неутралност).
    "JP_RPPI": ("OPT", 0.0, 4.0, 4.0),
}

# Provenance на всяка OPT зона — машинно четимият близнак на данни-паса.
# Числото пътува с обяснението си до лицето и до briefing_context.
OPT_PROVENANCE: dict[str, str] = {
    "JP_RPPI": (
        "доходната котва (абеномика max 3.91 → 4.0); балонът 1985-90 свети "
        "16/24, дефлацията 0/72 и абеномиката 0/28 мълчат; текущата ера е "
        "топла 8/16 — БЕЗ кредитен крак (двойката неактивна)"
    ),
    "JP_CREDIT_GDP_NFC": (
        "темпът на credit/GDP = кредит-минус-БВП разликата; котва абеномика "
        "max 4.14 → 4.5; балонът свети 10/24, абеномиката 0/28; ⚠ 2008Q4-"
        "2009Q3 (4/72) е GFC знаменателят, не бум"
    ),
    "JP_CREDIT_GDP_HH": (
        "същата котва (абеномика max 2.03 → 3.0); балонът свети 15/24, "
        "абеномиката 0/28, текущата 0/16 (домакинствата се разлистват); "
        "⚠ 2009Q2-2010Q1 (4/72) е GFC знаменателят"
    ),
}

# Кога и как са фиксирани зоните — един стринг, цитиран от лицето и експорта.
OPT_SOURCE_NOTE = (
    "Зоните са фиксирани от данни-пас на живо (05.08.2026, фаза 6) и не се "
    "калибрират наново: балонът 1985-90 свети (имоти 16/24 · кредит 10-15/24), "
    "дефлацията и абеномиката мълчат (0/72 · 0/28 извън GFC знаменателя)."
)

# Multi-lens override (лещово-специфична полярност). Празно за JP — всяка
# серия живее в точно една леща. Механизмът се пази заради фамилния паритет.
POLARITY_BY_LENS: dict[tuple[str, str], Any] = {}

# Peer-group тегла. Групите са по 1-2 серии → всички 1.0 (фамилен мандат №38
# §А3). Механизмът се пази, за да не се преправя агрегацията по-късно.
PEER_GROUP_WEIGHT: dict[str, float] = {}


def polarity_for(key: str, lens: str | None = None) -> Any:
    """Полярността на серия (по избор — в контекста на конкретна леща)."""
    if lens is not None and (lens, key) in POLARITY_BY_LENS:
        return POLARITY_BY_LENS[(lens, key)]
    return POLARITY.get(key, +1)


def peer_group_weight(pg: str) -> float:
    """Тегло на peer-група в лещата. Default 1.0."""
    return PEER_GROUP_WEIGHT.get(pg, 1.0)


def is_u_shaped(polarity: Any) -> bool:
    """True за U-полярност (лента около цел или около собствената норма)."""
    return isinstance(polarity, tuple) and bool(polarity) and polarity[0] == "U"


def is_opt(polarity: Any) -> bool:
    """True за bounded-optimum полярност `("OPT", lo, hi, s)`."""
    return (
        isinstance(polarity, tuple)
        and len(polarity) == 4
        and polarity[0] == "OPT"
    )


def opt_zone(polarity: Any) -> tuple[float, float, float]:
    """`("OPT", lo, hi, s)` → (lo, hi, s) като числа. Едно място за разопаковането."""
    _, lo, hi, s = polarity
    return float(lo), float(hi), float(s)


def opt_keys() -> list[str]:
    """Ключовете с оптимална зона — В РЕДА на POLARITY.

    ЕДИНСТВЕНИЯТ източник за „кои са бум-сериите": температурният слой се
    извежда оттук, не от собствен списък. В JP v1 е празен списък (фаза 6).
    """
    return [k for k, v in POLARITY.items() if is_opt(v)]
